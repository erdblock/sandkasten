extends ./view.jade

block content
	div(class="panel panel-danger")
		div(class="panel-heading")
			span(class="glyphicon glyphicon-warning-sign" aria-hidden="true")
			span(class="sr-only") Warning:
			| 
			| Delete
		div(class="panel-body")
			p
				| Are you sure that you want to delete the instance of the plugin
				| #{activePluginInstance.locals.title}?
			form(role="form" method="post")
				input(type="submit" value="Delete", class="btn btn-danger pull-right brMarginTop")
